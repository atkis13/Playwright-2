name: test

on:
  push:
    breanches:
      - 'main'

jobs:
  test:
    runs-on: ubuntu-latest
    steps:      
      -uses: actions\checkout@v3

      -name: run tests
      run: dotnet test
      
 
